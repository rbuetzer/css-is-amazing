import CodeSample from "../components/examples/the-checked-state.astro";

## The `:checked` selector

The `:checked` selector is often used to show and hide content in forms based on
the state of a checkbox or radio button, like in the following example:

<CodeSample />
